global WIDTH = 32;
global HEIGHT = 32;
global LEN = 5;
global S = [];

def conf(t) {
	//t.width  = WIDTH*12
	//t.height = HEIGHT*12
}


def load() {
	S = [[5],[5],[5],[5],[5]];
}

def draw() {
	//graphics.setColor(0, 255, 255);


	for (var x = 0; x < 32; ++x) {
		for (var y = 0; y < 32; ++y) {
			graphics.setColor(x * 3, 255, 255);
			graphics.rectangle("fill", x*12, y*12, 11, 11);
		}
	}

}
