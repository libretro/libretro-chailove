
global keyPressed;
global keyReleased;

def load() {
	keyPressed = "a"
	keyReleased = "a"
}

def draw() {
	graphics.print(keyPressed, 5, 20)
	graphics.print(keyReleased, 5, 100)
}

def keypressed(key, scancode) {
	keyPressed = key
}

def keyreleased(key, scancode) {
	keyReleased = key
}
