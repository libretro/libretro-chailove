global map

def load() {
	map = love.tiled.newMap("map/map.tmx")
}

def draw() {
	love.graphics.print("Width: " + to_string(map.GetWidth()), 100, 100)
	love.graphics.print("Height: " + to_string(map.GetHeight()), 100, 200)
}
